#include "main.h"

int _printf(const char *format, ...){
    int num_args = stringLen(format);

    va_list args;
    va_start(args,format);

    for(int j = 0; j < num_args;i++){
        if (format[i] == )
    }

}